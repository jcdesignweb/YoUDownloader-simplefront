import Button from './button';

class Previous extends Button {
    constructor(selectorId) {
        element = document.getElementById(selectorId);
        super();
    }

}

export default Previous;